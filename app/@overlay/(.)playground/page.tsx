export const metadata = { title: "Playground — Ben Sandivar" };

export default function PlaygroundOverlay() {
  return (
    <section style={{ textAlign: "center" }}>
      <div>
        <h1>Playground (Overlay)</h1>
        <p>We’ll load the Buck-It iframe here later.</p>
      </div>
    </section>
  );
}