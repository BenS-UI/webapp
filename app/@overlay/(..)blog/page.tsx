export const metadata = { title: "Blog — Ben Sandivar" };

export default function BlogOverlay() {
  return (
    <section style={{ textAlign: "center" }}>
      <div>
        <h1>Blog (Overlay)</h1>
        <p>Shown on top of Home when clicked there.</p>
      </div>
    </section>
  );
}
